import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "GovContracts Intel - Federal Data/AI Contract Intelligence",
  description: "Analyze government contracts in the data and AI space. Track competitors like Palantir, identify opportunities for Snowflake.",
  keywords: ["government contracts", "federal procurement", "data analytics", "AI contracts", "Palantir", "Snowflake"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
